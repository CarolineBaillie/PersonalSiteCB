---
const { title, desc, link, image } =  Astro.props;
---
<div class="card">
    <img class="cardImg" width="100%" height="100%"  src={image} />
    <h3>{title}</h3>
    <p>{desc}</p>
    <a href={link} target="_blank">Go to page</a>
</div>